window.SQL_MASTERY_MODULE = {
  "id": "cte-patterns",
  "title": "ctes",
  "learningPath": 5,
  "questionCount": 20,
  "database": "../../databases/ecommerce.sqlite",
  "storageKey": "sqlMasteryCtesV2",
  "patternGoal": {
    "recognize": [
      "multi-step",
      "first calculate",
      "then filter",
      "build a report",
      "make query readable"
    ],
    "think": [
      "WITH",
      "named result sets",
      "multiple CTEs",
      "step-by-step SQL"
    ]
  },
  "frameworkPrompts": [
    "What should the first CTE calculate?",
    "What grain should each CTE return?",
    "Does a later CTE depend on an earlier one?",
    "Which joins and aggregations belong inside the CTE?",
    "What should remain for the final SELECT?",
    "Would multiple CTEs make the logic clearer?",
    "Should missing values be handled with COALESCE?",
    "Does the final result require filtering, classification, or comparison?"
  ],
  "questions": [
    {
      "num": 1,
      "difficulty": "Beginner",
      "text": "Use a CTE to calculate the total revenue for each order.",
      "sql": "WITH order_revenue AS (\n  SELECT\n    order_id,\n    SUM(quantity * unit_price) AS total_revenue\n  FROM order_items\n  GROUP BY order_id\n)\nSELECT\n  order_id,\n  ROUND(total_revenue, 2) AS total_revenue\nFROM order_revenue;",
      "hints": [
        "Create a CTE that groups order_items by order_id.",
        "Revenue is quantity multiplied by unit_price.",
        "Select the calculated totals from the CTE."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "grouped-metric",
        "requiredColumns": [
          "order_id",
          "total_revenue"
        ],
        "optionalColumns": [],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 2,
      "difficulty": "Beginner",
      "text": "Use a CTE to identify orders with revenue above the average order revenue.",
      "sql": "WITH order_revenue AS (\n  SELECT\n    order_id,\n    SUM(quantity * unit_price) AS total_revenue\n  FROM order_items\n  GROUP BY order_id\n),\naverage_order_revenue AS (\n  SELECT AVG(total_revenue) AS average_revenue\n  FROM order_revenue\n)\nSELECT\n  o.order_id,\n  ROUND(o.total_revenue, 2) AS total_revenue\nFROM order_revenue AS o\nCROSS JOIN average_order_revenue AS a\nWHERE o.total_revenue > a.average_revenue;",
      "hints": [
        "First calculate one revenue total per order.",
        "Use a second CTE to calculate the average of those totals.",
        "Compare each order total with the average in the final query."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "single-metric",
        "requiredColumns": [
          "order_id",
          "total_revenue"
        ],
        "optionalColumns": [],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 3,
      "difficulty": "Beginner",
      "text": "Use a CTE to calculate the total revenue generated by each customer.",
      "sql": "WITH customer_revenue AS (\n  SELECT\n    c.customer_id,\n    c.customer_name,\n    COALESCE(SUM(oi.quantity * oi.unit_price), 0) AS total_revenue\n  FROM customers AS c\n  LEFT JOIN orders AS o\n    ON o.customer_id = c.customer_id\n  LEFT JOIN order_items AS oi\n    ON oi.order_id = o.order_id\n  GROUP BY c.customer_id, c.customer_name\n)\nSELECT\n  customer_id,\n  customer_name,\n  ROUND(total_revenue, 2) AS total_revenue\nFROM customer_revenue;",
      "hints": [
        "Start the CTE from customers so every customer can be included.",
        "Use LEFT JOIN and COALESCE for customers without purchases.",
        "Group by customer before selecting from the CTE."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "grouped-metric",
        "requiredColumns": [
          "customer_id",
          "total_revenue"
        ],
        "optionalColumns": [
          "customer_name"
        ],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 4,
      "difficulty": "Beginner",
      "text": "Use a CTE to identify high-value customers based on total revenue of at least 1000.",
      "sql": "WITH customer_revenue AS (\n  SELECT\n    c.customer_id,\n    c.customer_name,\n    COALESCE(SUM(oi.quantity * oi.unit_price), 0) AS total_revenue\n  FROM customers AS c\n  LEFT JOIN orders AS o\n    ON o.customer_id = c.customer_id\n  LEFT JOIN order_items AS oi\n    ON oi.order_id = o.order_id\n  GROUP BY c.customer_id, c.customer_name\n)\nSELECT\n  customer_id,\n  customer_name,\n  ROUND(total_revenue, 2) AS total_revenue\nFROM customer_revenue\nWHERE total_revenue >= 1000;",
      "hints": [
        "Reuse a customer revenue CTE.",
        "Filter the calculated result in the outer query.",
        "The high-value threshold is 1000."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "identify-entity",
        "requiredColumns": [
          "customer_id"
        ],
        "optionalColumns": [
          "customer_name"
        ],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 5,
      "difficulty": "Beginner",
      "text": "Use a CTE to calculate total revenue for each product.",
      "sql": "WITH product_revenue AS (\n  SELECT\n    p.product_id,\n    p.product_name,\n    COALESCE(SUM(oi.quantity * oi.unit_price), 0) AS total_revenue\n  FROM products AS p\n  LEFT JOIN order_items AS oi\n    ON oi.product_id = p.product_id\n  GROUP BY p.product_id, p.product_name\n)\nSELECT\n  product_id,\n  product_name,\n  ROUND(total_revenue, 2) AS total_revenue\nFROM product_revenue;",
      "hints": [
        "Start from products to keep products without sales.",
        "Aggregate line-item revenue by product.",
        "Select the product totals from the CTE."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "grouped-metric",
        "requiredColumns": [
          "product_id",
          "total_revenue"
        ],
        "optionalColumns": [
          "product_name"
        ],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 6,
      "difficulty": "Easy",
      "text": "Use a CTE to identify the product with the highest total revenue.",
      "sql": "WITH product_revenue AS (\n  SELECT\n    p.product_id,\n    p.product_name,\n    SUM(oi.quantity * oi.unit_price) AS total_revenue\n  FROM products AS p\n  INNER JOIN order_items AS oi\n    ON oi.product_id = p.product_id\n  GROUP BY p.product_id, p.product_name\n)\nSELECT\n  product_id,\n  product_name,\n  ROUND(total_revenue, 2) AS total_revenue\nFROM product_revenue\nWHERE total_revenue = (\n  SELECT MAX(total_revenue)\n  FROM product_revenue\n);",
      "hints": [
        "Calculate product revenue inside the CTE.",
        "Find the maximum CTE value in a scalar subquery.",
        "Use equality to preserve ties."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "identify-entity",
        "requiredColumns": [
          "product_id"
        ],
        "optionalColumns": [
          "product_name"
        ],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 7,
      "difficulty": "Easy",
      "text": "Use multiple CTEs to calculate customer revenue and compare it to the average customer revenue.",
      "sql": "WITH customer_revenue AS (\n  SELECT\n    c.customer_id,\n    c.customer_name,\n    COALESCE(SUM(oi.quantity * oi.unit_price), 0) AS total_revenue\n  FROM customers AS c\n  LEFT JOIN orders AS o\n    ON o.customer_id = c.customer_id\n  LEFT JOIN order_items AS oi\n    ON oi.order_id = o.order_id\n  GROUP BY c.customer_id, c.customer_name\n),\naverage_customer_revenue AS (\n  SELECT AVG(total_revenue) AS average_revenue\n  FROM customer_revenue\n)\nSELECT\n  c.customer_id,\n  c.customer_name,\n  ROUND(c.total_revenue, 2) AS total_revenue,\n  ROUND(a.average_revenue, 2) AS average_customer_revenue\nFROM customer_revenue AS c\nCROSS JOIN average_customer_revenue AS a\nWHERE c.total_revenue > a.average_revenue;",
      "hints": [
        "The first CTE should return one revenue total per customer.",
        "The second CTE should calculate the average of those totals.",
        "Join both CTE results in the final query and compare them."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "single-metric",
        "requiredColumns": [
          "customer_id",
          "total_revenue",
          "average_customer_revenue"
        ],
        "optionalColumns": [
          "customer_name"
        ],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 8,
      "difficulty": "Easy",
      "text": "Use a CTE to calculate monthly revenue.",
      "sql": "WITH monthly_revenue AS (\n  SELECT\n    strftime('%Y-%m', o.order_date) AS revenue_month,\n    SUM(oi.quantity * oi.unit_price) AS total_revenue\n  FROM orders AS o\n  INNER JOIN order_items AS oi\n    ON oi.order_id = o.order_id\n  GROUP BY strftime('%Y-%m', o.order_date)\n)\nSELECT\n  revenue_month,\n  ROUND(total_revenue, 2) AS total_revenue\nFROM monthly_revenue\nORDER BY revenue_month;",
      "hints": [
        "Join orders to order_items to access dates and revenue.",
        "Use strftime('%Y-%m', order_date) in SQLite.",
        "Group inside the CTE and order the final result by month."
      ],
      "orderSensitive": true,
      "validation": {
        "type": "single-metric",
        "requiredColumns": [
          "revenue_month",
          "total_revenue"
        ],
        "optionalColumns": [],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": true
      }
    },
    {
      "num": 9,
      "difficulty": "Easy",
      "text": "Use a CTE to calculate total revenue by product category.",
      "sql": "WITH category_revenue AS (\n  SELECT\n    p.category,\n    SUM(oi.quantity * oi.unit_price) AS total_revenue\n  FROM products AS p\n  INNER JOIN order_items AS oi\n    ON oi.product_id = p.product_id\n  GROUP BY p.category\n)\nSELECT\n  category,\n  ROUND(total_revenue, 2) AS total_revenue\nFROM category_revenue;",
      "hints": [
        "Join products to order_items.",
        "Group by the product category inside the CTE.",
        "Return the category totals from the CTE."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "grouped-metric",
        "requiredColumns": [
          "category",
          "total_revenue"
        ],
        "optionalColumns": [],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 10,
      "difficulty": "Easy",
      "text": "Use a CTE to identify product categories with revenue above the average category revenue.",
      "sql": "WITH category_revenue AS (\n  SELECT\n    p.category,\n    SUM(oi.quantity * oi.unit_price) AS total_revenue\n  FROM products AS p\n  INNER JOIN order_items AS oi\n    ON oi.product_id = p.product_id\n  GROUP BY p.category\n),\naverage_category_revenue AS (\n  SELECT AVG(total_revenue) AS average_revenue\n  FROM category_revenue\n)\nSELECT\n  c.category,\n  ROUND(c.total_revenue, 2) AS total_revenue\nFROM category_revenue AS c\nCROSS JOIN average_category_revenue AS a\nWHERE c.total_revenue > a.average_revenue;",
      "hints": [
        "First calculate category totals.",
        "Then calculate the average category total in a second CTE.",
        "Filter above-average categories in the final query."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "single-metric",
        "requiredColumns": [
          "category",
          "total_revenue"
        ],
        "optionalColumns": [],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 11,
      "difficulty": "Intermediate",
      "text": "Use a CTE to calculate revenue generated from Completed orders only.",
      "sql": "WITH completed_revenue AS (\n  SELECT\n    SUM(oi.quantity * oi.unit_price) AS total_revenue\n  FROM orders AS o\n  INNER JOIN order_items AS oi\n    ON oi.order_id = o.order_id\n  WHERE o.status = 'Completed'\n)\nSELECT\n  ROUND(total_revenue, 2) AS completed_order_revenue\nFROM completed_revenue;",
      "hints": [
        "Filter Completed orders inside the CTE.",
        "Join order_items to calculate revenue.",
        "Return the single total from the CTE."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "single-metric",
        "requiredColumns": [
          "completed_order_revenue"
        ],
        "optionalColumns": [],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 12,
      "difficulty": "Intermediate",
      "text": "Use a CTE to calculate the order cancellation rate.",
      "sql": "WITH order_summary AS (\n  SELECT\n    COUNT(*) AS total_orders,\n    SUM(CASE WHEN status = 'Cancelled' THEN 1 ELSE 0 END) AS cancelled_orders\n  FROM orders\n)\nSELECT\n  total_orders,\n  cancelled_orders,\n  ROUND(100.0 * cancelled_orders / NULLIF(total_orders, 0), 2) AS cancellation_rate\nFROM order_summary;",
      "hints": [
        "The CTE should return total and Cancelled order counts.",
        "Use conditional aggregation for cancelled_orders.",
        "Calculate the percentage in the outer query."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "percentage-rate",
        "requiredColumns": [
          "total_orders",
          "cancelled_orders",
          "cancellation_rate"
        ],
        "optionalColumns": [],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 13,
      "difficulty": "Intermediate",
      "text": "Use a CTE to identify customers who have never placed an order.",
      "sql": "WITH customers_with_orders AS (\n  SELECT DISTINCT customer_id\n  FROM orders\n)\nSELECT\n  c.customer_id,\n  c.customer_name,\n  c.country\nFROM customers AS c\nLEFT JOIN customers_with_orders AS cwo\n  ON cwo.customer_id = c.customer_id\nWHERE cwo.customer_id IS NULL;",
      "hints": [
        "Create a CTE containing customer IDs that appear in orders.",
        "LEFT JOIN that list to all customers.",
        "Keep rows where the CTE match is NULL."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "single-metric",
        "requiredColumns": [
          "customer_id",
          "country"
        ],
        "optionalColumns": [
          "customer_name"
        ],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 14,
      "difficulty": "Intermediate",
      "text": "Use a CTE to identify products that have never been ordered.",
      "sql": "WITH ordered_products AS (\n  SELECT DISTINCT product_id\n  FROM order_items\n)\nSELECT\n  p.product_id,\n  p.product_name,\n  p.price\nFROM products AS p\nLEFT JOIN ordered_products AS op\n  ON op.product_id = p.product_id\nWHERE op.product_id IS NULL;",
      "hints": [
        "Create a CTE of product IDs found in order_items.",
        "Join the CTE to all products with LEFT JOIN.",
        "Missing matches identify products that were never ordered."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "single-metric",
        "requiredColumns": [
          "product_id",
          "price"
        ],
        "optionalColumns": [
          "product_name"
        ],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 15,
      "difficulty": "Intermediate",
      "text": "Use a CTE to classify customers into High Value, Medium Value, and Low Value revenue segments using thresholds of 1000 and 300.",
      "sql": "WITH customer_revenue AS (\n  SELECT\n    c.customer_id,\n    c.customer_name,\n    COALESCE(SUM(oi.quantity * oi.unit_price), 0) AS total_revenue\n  FROM customers AS c\n  LEFT JOIN orders AS o\n    ON o.customer_id = c.customer_id\n  LEFT JOIN order_items AS oi\n    ON oi.order_id = o.order_id\n  GROUP BY c.customer_id, c.customer_name\n)\nSELECT\n  customer_id,\n  customer_name,\n  ROUND(total_revenue, 2) AS total_revenue,\n  CASE\n    WHEN total_revenue >= 1000 THEN 'High Value'\n    WHEN total_revenue >= 300 THEN 'Medium Value'\n    ELSE 'Low Value'\n  END AS revenue_segment\nFROM customer_revenue;",
      "hints": [
        "Calculate customer revenue in the CTE.",
        "Apply the CASE expression in the outer query.",
        "Test the highest threshold first."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "single-metric",
        "requiredColumns": [
          "customer_id",
          "revenue_segment"
        ],
        "optionalColumns": [
          "customer_name"
        ],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 16,
      "difficulty": "Advanced",
      "text": "Use a CTE to calculate each customer's percentage contribution to total revenue.",
      "sql": "WITH customer_revenue AS (\n  SELECT\n    c.customer_id,\n    c.customer_name,\n    COALESCE(SUM(oi.quantity * oi.unit_price), 0) AS total_revenue\n  FROM customers AS c\n  LEFT JOIN orders AS o\n    ON o.customer_id = c.customer_id\n  LEFT JOIN order_items AS oi\n    ON oi.order_id = o.order_id\n  GROUP BY c.customer_id, c.customer_name\n),\noverall_revenue AS (\n  SELECT SUM(total_revenue) AS total_revenue\n  FROM customer_revenue\n)\nSELECT\n  c.customer_id,\n  c.customer_name,\n  ROUND(c.total_revenue, 2) AS total_revenue,\n  ROUND(100.0 * c.total_revenue / NULLIF(o.total_revenue, 0), 2) AS revenue_percentage\nFROM customer_revenue AS c\nCROSS JOIN overall_revenue AS o;",
      "hints": [
        "Calculate customer totals in the first CTE.",
        "Sum those totals in a second CTE.",
        "Divide each customer's total by the overall total."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "percentage-rate",
        "requiredColumns": [
          "customer_id",
          "total_revenue",
          "revenue_percentage"
        ],
        "optionalColumns": [
          "customer_name"
        ],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 17,
      "difficulty": "Advanced",
      "text": "Use a CTE to calculate each product's percentage contribution to total revenue.",
      "sql": "WITH product_revenue AS (\n  SELECT\n    p.product_id,\n    p.product_name,\n    COALESCE(SUM(oi.quantity * oi.unit_price), 0) AS total_revenue\n  FROM products AS p\n  LEFT JOIN order_items AS oi\n    ON oi.product_id = p.product_id\n  GROUP BY p.product_id, p.product_name\n),\noverall_revenue AS (\n  SELECT SUM(total_revenue) AS total_revenue\n  FROM product_revenue\n)\nSELECT\n  p.product_id,\n  p.product_name,\n  ROUND(p.total_revenue, 2) AS total_revenue,\n  ROUND(100.0 * p.total_revenue / NULLIF(o.total_revenue, 0), 2) AS revenue_percentage\nFROM product_revenue AS p\nCROSS JOIN overall_revenue AS o;",
      "hints": [
        "Create one revenue total per product.",
        "Calculate the grand total in a second CTE.",
        "Use the two CTEs together in the final percentage calculation."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "percentage-rate",
        "requiredColumns": [
          "product_id",
          "total_revenue",
          "revenue_percentage"
        ],
        "optionalColumns": [
          "product_name"
        ],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 18,
      "difficulty": "Advanced",
      "text": "Use multiple CTEs to compare revenue between June and July.",
      "sql": "WITH monthly_revenue AS (\n  SELECT\n    strftime('%m', o.order_date) AS order_month,\n    SUM(oi.quantity * oi.unit_price) AS total_revenue\n  FROM orders AS o\n  INNER JOIN order_items AS oi\n    ON oi.order_id = o.order_id\n  WHERE strftime('%m', o.order_date) IN ('06', '07')\n  GROUP BY strftime('%m', o.order_date)\n),\njune_revenue AS (\n  SELECT COALESCE(total_revenue, 0) AS revenue\n  FROM monthly_revenue\n  WHERE order_month = '06'\n),\njuly_revenue AS (\n  SELECT COALESCE(total_revenue, 0) AS revenue\n  FROM monthly_revenue\n  WHERE order_month = '07'\n)\nSELECT\n  ROUND(june_revenue.revenue, 2) AS june_revenue,\n  ROUND(july_revenue.revenue, 2) AS july_revenue,\n  ROUND(july_revenue.revenue - june_revenue.revenue, 2) AS revenue_change\nFROM june_revenue\nCROSS JOIN july_revenue;",
      "hints": [
        "First calculate revenue by month.",
        "Use separate CTEs to isolate June and July.",
        "Compare the two values in the final SELECT."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "single-metric",
        "requiredColumns": [
          "june_revenue",
          "july_revenue",
          "revenue_change"
        ],
        "optionalColumns": [],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 19,
      "difficulty": "Advanced",
      "text": "Use a CTE to calculate the average order value for each country.",
      "sql": "WITH order_revenue AS (\n  SELECT\n    o.order_id,\n    c.country,\n    SUM(oi.quantity * oi.unit_price) AS total_revenue\n  FROM orders AS o\n  INNER JOIN customers AS c\n    ON c.customer_id = o.customer_id\n  INNER JOIN order_items AS oi\n    ON oi.order_id = o.order_id\n  GROUP BY o.order_id, c.country\n)\nSELECT\n  country,\n  ROUND(AVG(total_revenue), 2) AS average_order_value\nFROM order_revenue\nGROUP BY country;",
      "hints": [
        "The CTE should produce one total per order and include country.",
        "Do not average line-item values directly.",
        "Average the order totals by country in the outer query."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "grouped-metric",
        "requiredColumns": [
          "country",
          "average_order_value"
        ],
        "optionalColumns": [],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    },
    {
      "num": 20,
      "difficulty": "Advanced",
      "text": "Use multiple CTEs to build a business summary report showing total customers, total orders, total revenue, and average order value.",
      "sql": "WITH customer_summary AS (\n  SELECT COUNT(*) AS total_customers\n  FROM customers\n),\norder_revenue AS (\n  SELECT\n    order_id,\n    SUM(quantity * unit_price) AS total_revenue\n  FROM order_items\n  GROUP BY order_id\n),\norder_summary AS (\n  SELECT\n    COUNT(*) AS total_orders,\n    SUM(total_revenue) AS total_revenue,\n    AVG(total_revenue) AS average_order_value\n  FROM order_revenue\n)\nSELECT\n  c.total_customers,\n  o.total_orders,\n  ROUND(o.total_revenue, 2) AS total_revenue,\n  ROUND(o.average_order_value, 2) AS average_order_value\nFROM customer_summary AS c\nCROSS JOIN order_summary AS o;",
      "hints": [
        "Use one CTE for the customer count.",
        "Use another CTE to calculate one revenue total per order.",
        "Build the final order summary from those order totals, then combine the results."
      ],
      "orderSensitive": false,
      "validation": {
        "type": "single-metric",
        "requiredColumns": [
          "total_customers",
          "total_orders",
          "total_revenue",
          "average_order_value"
        ],
        "optionalColumns": [],
        "allowExtraColumns": true,
        "ignoreColumnNames": true,
        "numericTolerance": 0.011,
        "orderSensitive": false
      }
    }
  ]
};
